import {StyleSheet} from 'react-native';

import {orange} from '../../../styles/colors';

const gamePointsScreenStyles = StyleSheet.create({
	container: {
		backgroundColor: orange,
		alignItems: 'center',
		flex: 1,
		justifyContent: 'center',
	}
});

export default gamePointsScreenStyles;
